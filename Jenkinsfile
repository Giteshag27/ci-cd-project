pipeline {
    agent any

    stages {
        stage('Clone Code') {
            steps {
                echo 'Cloning happens automatically in Jenkins'
            }
        }

        stage('Build Step') {
            steps {
                echo 'Pipeline is working 🚀'
            }
        }
    }
}